import 'package:alarmrev/model/alarm.dart';
import 'package:flutter/material.dart';

class AlarmRingingPage extends StatelessWidget {
  AlarmRingingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
